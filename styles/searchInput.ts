import { StyleSheet } from "react-native";

export default StyleSheet.create({
  searchBar: {
    backgroundColor: "#fff",
    borderRadius: 50,
    height: 40,
    width: "100%",
    flexDirection: "row",
    alignItems: "center",
    paddingHorizontal: 10,
    marginVertical: 10,
  },
});
